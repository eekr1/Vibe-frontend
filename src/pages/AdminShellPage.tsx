export function AdminShellPage() {
  return (
    <section className="surface-panel wide-panel">
      <p className="eyebrow">Admin</p>
      <h2>Operational routes are reserved.</h2>
      <p>Admin implementation stays parked until mature platform truth exists in later waves.</p>
    </section>
  );
}
